---
document: v1.0.9_CODE_TRACKER.md
project: Easing Curve
release: 1.0.9-dev
document_type: release-code-tracker
requirements: test/docs/_test_plans/_CODE_WORKFLOW_REQUIREMENTS.md
plan_status: frozen
execution_status: not_started
baseline_commit: 91777e540c303b91936e0a9902f73856fdd4d2d4
current_item: AUDIT-01
current_step: none
created: 2026-08-27
last_updated: 2026-08-27
---

# Easing Curve v1.0.9 Code Tracker

## 1. Release Charter [FROZEN]

### 1.1 User-confirmed v1.0.9 goals

The user confirmed the following goals for v1.0.9:

1. **Code-smells / architecture / performance / bug review**
   - identify code smells and architecture improvements for future
     maintainability;
   - identify meaningful speed/performance bottlenecks and worthwhile
     optimizations;
   - identify concrete program bugs discovered during the review;
   - do not assume every finding must be implemented in v1.0.9.

2. **Graph-toolbar point navigation / reordering controls**
   - add graph-toolbar arrows for selecting and/or reordering points;
   - if both navigation and reordering are exposed, keep their semantics
     visibly distinct rather than making one ambiguous arrow operation;
   - reuse existing point ordering, selection, identity, and Undo/Redo
     behavior rather than creating a second ordering model.

3. **Persist graph zoom and pan through project/editor restarts**
   - store graph view state with the `EasingCurve` resource so it survives
     editor/project restarts;
   - preserve the current per-resource ownership model;
   - review serialization/resource-dirty/source-control implications before
     implementation;
   - preferred direction from the accepted feature review is private
     storage-only resource state rather than visible public curve parameters.

4. **Curve-editor playback/sample preview**
   - add a playback bar, playhead, dot, or equivalent preview UI to the curve
     editor;
   - the preview must make the current normalized X value and the corresponding
     `EasingCurve.sample(x)` output visually understandable;
   - keep v1.0.9 preview focused on the easing curve itself rather than
     animating arbitrary scene properties.

### 1.2 Explicit v1.0.9 non-goals

The following user-confirmed future work is outside v1.0.9 and must not be
bundled into current feature work:

- multi-point selection/editing;
- Shift-drag selection box;
- bulk point deletion;
- bulk Handle Mode / control-state updates;
- multi-point translation;
- Node/property animation assignment;
- position/rotation/scale target-track behavior;
- general animation timing/speed/duration model tied to target assignment;
- turning `EasingCurve` itself into an AnimationPlayer-style track container.

A v1.0.9 feature may preserve architectural/input room for those future items,
but must not implement them without an explicit release-scope amendment.

### 1.3 Baseline facts established before AUDIT-01

| Field | Baseline |
| --- | --- |
| Branch | `dev` |
| HEAD | `91777e540c303b91936e0a9902f73856fdd4d2d4` |
| HEAD history entry | `archive docs` |
| Plugin version | `1.0.9-dev` |
| Active Godot | `4.7.1-stable (official)` |
| Active project | `C:/Godot/_dev/_plugins/easing_curve/` |
| Active scene | `res://addons/easing_curve/test_scene/test.tscn` |
| Play state at tracker generation | stopped |
| Authoritative full runner | `test/scripts/run_all_tests.ps1` |
| Registered suites | 17 total: 8 headless + 9 Editor-host |
| Fresh v1.0.9 full-suite baseline | **PENDING** |
| Previous v1.0.8 release-gate result | 17/17 PASS on Godot 4.7.1 |
| Production code modified while creating this tracker | No |

#### Fresh baseline limitation

The active MCP interface does not expose arbitrary host PowerShell execution,
and its generic test runner is not the project's authoritative
`res://test`/PowerShell manifest. Therefore no unrelated runner is substituted
for `test/scripts/run_all_tests.ps1`.

A fresh v1.0.9 17-suite baseline remains a required pre-implementation gate and
should be executed as part of the release-baseline/AUDIT-01 start when the
host runner is available.

#### Worktree note

This tracker-generation task intentionally changes only planning/documentation
files. Exact `git status` output is not available through the active MCP
filesystem interface, so the tracker does not claim the worktree is otherwise
clean. Verify status before beginning production implementation.

### 1.4 Relevant inherited v1.0.8 baseline

v1.0.8 closed with Shift axis-constrained point/handle dragging implemented and
validated. The feature release gate reported:

- 17/17 registered suites passing on Godot 4.7.1;
- `git diff --check` passing;
- 4,908 reported checks across the registered suite;
- no timeout or `SCRIPT ERROR` in the authoritative runner;
- pre-held Shift intentionally reserved rather than consumed for the initial
  drag hold, leaving design space for future multi-selection behavior.

Treat v1.0.8 completed behavior as the regression baseline unless AUDIT-01 finds
a demonstrated defect.

### 1.5 Preservation / compatibility constraints

Unless a later approved work item explicitly changes one of these contracts,
v1.0.9 must preserve:

- `EasingCurve.sample()` normalized interpolation semantics;
- existing transition/ease enum numeric values;
- existing public classes, properties, methods, signals, and constants;
- serialized point data and existing saved-resource compatibility;
- point Resource identity/order semantics;
- existing point/control Handle Modes and Force Linear behavior;
- existing point/control locks;
- endpoint takeover and point-order behavior;
- Shift axis-constrained dragging from v1.0.8;
- pending-add and RMB-delete behavior;
- MMB pan, wheel zoom, autofit, and zoom-around-pointer behavior;
- Inspector/graph selection synchronization;
- one coherent Undo/Redo action for completed graph/Inspector edits;
- explicit test-runner manifest and headless/Editor-host execution modes;
- `EasingCurve` as the interpolation/geometry resource rather than an
  AnimationPlayer replacement.

Any v1.0.9 persistence change must be designed so loading older resources that
do not contain the new optional view-state storage remains valid and defaults
to the existing normal view.

### 1.6 Planned v1.0.9 review / implementation order

```text
AUDIT-01
  -> release-level code-smells / architecture / performance / bug audit
  -> classify findings as required / optional / deferred / leave-alone

POINT-NAV-01
  -> graph-toolbar point selection/reorder controls

VIEW-STATE-01
  -> persistent per-resource zoom/pan across restarts

PREVIEW-01
  -> normalized curve sample/playhead preview

RELEASE-01
  -> final release-candidate validation / packaging / compatibility closeout
```

The audit is intentionally first so any real blocker or architecture issue can
be addressed before additional editor UI accumulates around it.

The audit must not automatically expand v1.0.9. New findings require explicit
classification and user approval before implementation.

### 1.7 Release acceptance criteria

Before v1.0.9 is considered release-ready:

- all approved v1.0.9 work items are complete or explicitly deferred;
- the authoritative 17-suite runner passes under its existing criteria;
- `git diff --check` passes;
- final Git status/diff contains only approved release changes;
- visible-editor smoke covers changed graph/Inspector interactions;
- serialized/saved-resource compatibility is checked for any view-state
  persistence change;
- older resources without persisted view state still load with valid defaults;
- graph preview does not alter the underlying curve merely by playing/scrubbing;
- v1.0.8 axis drag, pan/zoom, point reorder, selection, and Undo/Redo contracts
  remain intact;
- compatibility testing is repeated for every Godot version claimed by the
  release;
- release package/export contents contain only intended plugin/example files;
- plugin/version/release metadata is correct.

---

## 2. Future Release Roadmap [FROZEN USER-SUPPLIED BOUNDARY]

The user explicitly moved the following features out of v1.0.9. These entries
preserve the agreed direction but are not active authorization.

### 2.1 Future / v1.0.10 — `MULTI-EDIT-01` — Multi-point editing

**Status:** FUTURE / NOT ACTIVE

#### User goal

Support multi-point editing, with Shift-drag potentially acting as a selection
rectangle. Intended batch operations include:

- selecting multiple graph points;
- deleting selected points;
- updating Handle Mode across the selection;
- updating control state/mode across the selection;
- translating selected points together.

#### Agreed release rationale

This should be treated as a dedicated release-level feature rather than folded
into v1.0.9 because current graph/Inspector selection is fundamentally
single-point oriented.

The difficult part is not drawing the selection rectangle; it is introducing a
coherent multi-selection model across:

- graph selection/highlighting;
- Inspector state;
- point Resource identity through reorder;
- batch translation/order/endpoint behavior;
- batch Handle Mode/control-state editing;
- atomic Undo/Redo;
- selection restoration through Undo/Redo and Inspector refresh.

#### Existing architectural reservation

v1.0.8 deliberately leaves pre-held Shift unconsumed by the initial axis-drag
hold. Preserve that reservation unless a later multi-selection design explicitly
changes it.

### 2.2 Future / v1.0.10 or later — `ANIMATION-TARGET-01` — Node/property assignment

**Status:** FUTURE / NOT ACTIVE

#### User goal

Add AnimationPlayer-like assignment of easing behavior to a Node/property, such
as:

- position;
- rotation;
- scale;
- other supported animatable properties.

#### Agreed architectural direction

Do not assume Node/property target metadata belongs directly on `EasingCurve`.
`EasingCurve` should remain the reusable interpolation/geometry resource.

A future design should evaluate a separate track/animation abstraction, for
example conceptually:

```text
EasingCurve
    -> normalized interpolation

Easing animation/track resource
    -> target Node/property
    -> from/to values
    -> timing
    -> EasingCurve reference
```

The exact class/resource/API is not yet frozen and requires its own architecture
review before implementation.

#### Major design questions for that future release

- target path ownership and root-relative semantics;
- node rename/path resilience;
- local versus global transforms;
- supported property types;
- from/to value ownership and absolute/relative modes;
- reusable curves across multiple targets;
- safe editor preview/revert behavior;
- runtime versus editor-only responsibility;
- Undo/Redo behavior;
- external `.tres` resources shared across scenes.

### 2.3 Future / v1.0.10 or later — `ANIMATION-TIMING-01` — Duration / speed

**Status:** FUTURE / NOT ACTIVE

#### User goal

Add speed/duration parameters for animations.

#### Agreed architectural direction

Duration/speed does not transform the easing function in the same way as
`reverse` or `invert` and therefore should not be treated as an `EasingCurve`
Global Transform merely because that Inspector group already exists.

Normalized easing remains conceptually:

```text
elapsed time / duration
    -> normalized x
    -> EasingCurve.sample(x)
```

Timing should therefore live with the future preview/animation consumer or
future animation/track abstraction rather than becoming intrinsic sampling
state on reusable `EasingCurve`.

`ANIMATION-TIMING-01` depends on the future animation-target/track architecture.
Avoid storing both independent authoritative speed and duration values unless a
clear synchronization contract is designed.

---

## 3. Execution Dashboard [MUTABLE]

| Plan ID | Status | Current Step | Result / Summary | Commit |
| --- | --- | --- | --- | --- |
| `AUDIT-01` | NOT STARTED | none | Release-level smells/architecture/performance/bug review is next | — |
| `POINT-NAV-01` | NOT STARTED | none | Graph toolbar point selection/reorder controls | — |
| `VIEW-STATE-01` | NOT STARTED | none | Persist per-resource zoom/pan through restarts | — |
| `PREVIEW-01` | NOT STARTED | none | Curve-only normalized X/sample preview | — |
| `RELEASE-01` | NOT STARTED | none | Final release candidate validation | — |

### Current state

- **Current Plan ID:** `AUDIT-01`
- **Current step:** none; audit has not started
- **Next approval gate:** begin v1.0.9 baseline code-smells / architecture /
  performance / bug audit
- **Blocking issues:** none established yet
- **Fresh authoritative v1.0.9 test baseline:** pending
- **Production code changes authorized by tracker creation:** none
- **Future roadmap items active:** none

---

## 4. Work Items

### 4.1 `AUDIT-01` — v1.0.9 maintainability / architecture / performance / bug audit

**Status:** NOT STARTED

#### Objective

Review the post-v1.0.8 codebase for:

- concrete code smells affecting maintainability;
- architecture improvements worth considering;
- actual speed/performance bottlenecks or avoidable repeated work;
- concrete program bugs discovered during inspection;
- test/characterization gaps that materially increase feature risk.

#### Scope

The audit should cover the current plugin architecture broadly enough to assess
v1.0.9 feature risk, with particular attention to code touched heavily across
v1.0.6-v1.0.8:

- `EasingCurve` resource/state/snapshot responsibilities;
- graph input and drawing paths;
- Inspector point editing / selection / mutation coordination;
- Undo/Redo integration;
- point ordering and Resource identity;
- transition/function metadata;
- notification/property-list refresh behavior;
- test runner and Editor-host characterization;
- rendering/input/update hot paths where performance work could have practical
  value.

This list is an audit boundary, not a statement that these areas contain known
new defects.

#### Required audit output

Classify findings as:

- **must address before v1.0.9 feature work**;
- **worth addressing in v1.0.9 if separately approved**;
- **defer to future maintenance**;
- **leave alone / intentionally preserved**;
- **bug**;
- **performance opportunity**;
- **test/coverage gap**.

Assign stable Plan IDs to actionable findings only after evidence is gathered.
Do not turn every large method or duplicated-looking path into an automatic
refactor task.

#### Pre-audit baseline gate

Before production implementation begins, obtain a fresh authoritative
`test/scripts/run_all_tests.ps1` result for v1.0.9 when the host runner is
available and record any changed diagnostics.

#### Execution plan

**Not generated yet.**

Per user instruction, stop before performing this audit.

---

### 4.2 `POINT-NAV-01` — Graph-toolbar point selection / reordering controls

**Status:** NOT STARTED

#### Objective

Expose efficient point navigation and/or reordering controls directly in the
graph point toolbar.

#### Existing architecture to reuse

Current code already has mature Inspector-side point operations for:

- previous/next swap via move-up/move-down;
- wraparound first/last behavior;
- drag reorder;
- stable point Resource identity;
- selection restoration;
- Undo/Redo.

The graph toolbar already tracks a selected point and contains point-specific
controls.

#### Design constraint

Selection navigation and point reordering are different operations.

If both are included, the UI must make the difference clear:

- previous/next selection changes selection only;
- move previous/next changes point ordering and uses the existing reorder/Undo
  model.

Do not introduce a second independent ordering implementation inside the graph
editor.

#### Open design decision

During work-item planning, settle the minimal button set and icon/tooltip layout
that fits the compact graph toolbar without creating ambiguity.

#### Additional validation

In addition to standard closeout, protect:

- first/last wrap behavior if reorder arrows expose it;
- selection-only navigation without geometry changes;
- reordered point Resource identity;
- Inspector/graph selection synchronization;
- one Undo action per reorder;
- Undo/Redo selection restoration;
- no effect in Function mode where point editing is unavailable.

#### Execution plan

Not generated yet. Plan after `AUDIT-01` is reviewed/classified.

---

### 4.3 `VIEW-STATE-01` — Persistent EasingCurve graph zoom/pan

**Status:** NOT STARTED

#### Objective

Persist each `EasingCurve` resource's graph zoom and pan across project/editor
restarts.

#### Existing architecture

The resource already owns in-memory per-resource view state:

- `_last_slider_value`;
- `_last_zoom`;
- `_last_pan`.

The Inspector restores that state when rebuilding the curve editor, so
Inspector refresh and resource switching already preserve view state within the
same session.

#### Agreed direction

Preserve `EasingCurve` as the owner and persist the state as private
storage-only resource data rather than presenting zoom/pan as normal public
curve parameters.

#### Important design consequence to review before implementation

Persisting editor view state with the Resource may make panning/zooming mark a
scene/resource dirty and may create source-control diffs even when curve
geometry is unchanged.

This is not automatically a defect; it is a consequence of the requested
resource-persistent model and must be explicitly validated/accepted during the
work-item design review.

#### Compatibility constraints

- existing older resources without the new storage must load successfully;
- missing values default to the existing normal view;
- view state must not affect `sample()` output;
- view state should remain hidden from ordinary runtime-facing Inspector
  parameters;
- no change to zoom formulas, limits, pan semantics, autofit, wheel behavior,
  or zoom-around-pointer unless separately approved.

#### Additional validation

- save resource with non-default zoom/pan;
- restart/reload and verify exact intended restoration;
- external `.tres` and scene-embedded resource behavior where applicable;
- older fixture/resource compatibility;
- resource-dirty/source-control behavior documented;
- Inspector refresh and A -> B -> A resource switching still work;
- autofit returns to the expected default and persists appropriately.

#### Execution plan

Not generated yet. Plan after `AUDIT-01` is reviewed/classified.

---

### 4.4 `PREVIEW-01` — Curve sample/playhead preview

**Status:** NOT STARTED

#### Objective

Add an editor preview that visually communicates the current normalized X value
and corresponding `EasingCurve.sample(x)` value.

Conceptually:

```text
normalized x
    -> EasingCurve.sample(x)
    -> graph position (x, sampled_y)
```

A playhead/dot/bar may move through X so the user can see how the current curve
maps time/progress to eased progress.

#### v1.0.9 scope boundary

This is a **curve preview**, not arbitrary scene-property animation.

Do not require or add:

- Node targets;
- property paths;
- position/rotation/scale tracks;
- from/to animation values;
- general animation track resources;
- future multi-selection.

#### Design questions to settle during planning

Choose the smallest useful preview interaction, including whether v1.0.9 needs:

- play/pause/reset versus a scrub-only control;
- a moving dot on the curve, a vertical playhead, or both;
- direct X scrubbing;
- loop behavior.

These are implementation/UI design questions, not authorization to turn the
preview into an animation system.

#### Behavioral constraints

- preview must use the same authoritative `sample()` semantics as runtime;
- work for both Bézier and Function transitions;
- previewing/scrubbing must not mutate curve geometry or transition parameters;
- changing the curve while preview is visible must update the sampled output;
- pan/zoom transforms must display the preview at the correct graph position;
- preview state should remain editor-only unless a separate persistence
  decision is approved.

#### Additional validation

- sampled point matches `sample(x)` at representative X values;
- endpoints;
- overshoot/invert/reverse/function cases;
- pan/zoom visual position;
- editing while preview is active;
- no Undo action and no resource dirtying caused solely by advancing playback
  unless explicitly designed otherwise.

#### Execution plan

Not generated yet. Plan after `AUDIT-01` is reviewed/classified.

---

### 4.5 `RELEASE-01` — v1.0.9 release closeout

**Status:** NOT STARTED

#### Objective

Perform final release-candidate validation after all approved v1.0.9 work items
are complete.

#### Expected closeout

- authoritative 17-suite runner;
- focused feature suites as needed;
- `git diff --check`;
- final diff/status scope review;
- visible-editor smoke;
- persisted resource compatibility checks;
- supported Godot-version compatibility checks;
- packaging/export inspection;
- final plugin/version/release metadata verification.

#### Execution plan

Generate only after active feature implementation is complete.

---

## 5. Plan Amendments [APPEND ONLY]

No amendments yet.

| Amendment ID | Date | Plan ID / Roadmap ID | Change | Reason | Approval |
| --- | --- | --- | --- | --- | --- |

---

## 6. Project Diagnostics / Follow-ups

### `DIAG-01` — Serialization temporary-directory/editor scan race

**Classification:** known non-blocking test-harness/editor diagnostic inherited
from v1.0.8.

Observed message:

`Cannot change to 'res://test/_serialization_transition_contract/' folder.`

The serialization test creates/removes a temporary directory under `res://` and
an already-open editor may race with that deletion while scanning. The
registered serialization suite still passed during v1.0.8 closeout.

Do not classify this as a v1.0.9 production defect unless behavior materially
changes. A separate future test-infrastructure improvement could consider
`user://` only if serialization coverage remains equivalent.

### `DIAG-02` — Live-editor GDScript hot-reload error code 43

**Classification:** known live-editor/hot-reload artifact inherited from v1.0.8.

During prior script saves, the already-running editor could report error code
43 while fresh Editor-host processes loaded and tested the same source
successfully.

Fresh-process compile/test results remain the source of truth when this specific
hot-reload artifact recurs unchanged.

---

## 7. Release Closeout

**Status:** NOT STARTED

This section will become the final release-gate execution record for
`RELEASE-01`.

No v1.0.9 release closeout has been performed yet.

---

## 8. Current Handoff

**Release:** `1.0.9-dev`

**Tracker:** created with user-confirmed current and future release boundaries.

**Current active Plan ID:** `AUDIT-01`.

**Current step:** none.

**Next action:** begin the v1.0.9 code-smells / architecture / performance / bug
audit only after the user authorizes continuation.

**Fresh authoritative test baseline:** pending; use
`test/scripts/run_all_tests.ps1`, not the unrelated generic MCP test runner.

**Do not start yet:** `POINT-NAV-01`, `VIEW-STATE-01`, `PREVIEW-01`.

**Future / not active:** `MULTI-EDIT-01`, `ANIMATION-TARGET-01`,
`ANIMATION-TIMING-01`.

**Production code modified by this tracker-generation task:** none.

**Planning/documentation files created or updated by this task:**

- `test/docs/_test_plans/_CODE_WORKFLOW_REQUIREMENTS.md`;
- `test/docs/_test_plans/v1.0.9_CODE_TRACKER.md`.

**Stop condition:** satisfied. The tracker is generated and the code-smells
audit has not been performed.
